<h1>Listing servidores</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Ip</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @servidores.each do |servidor| %>
  <tr>
    <td><%= servidor.nome %></td>
    <td><%= servidor.ip %></td>
    <td><%= link_to 'Show', servidor %></td>
    <td><%= link_to 'Edit', edit_servidor_path(servidor) %></td>
    <td><%= link_to 'Destroy', servidor, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Servidor', new_servidor_path %>
